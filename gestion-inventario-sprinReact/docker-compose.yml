services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - TZ=${TZ:-America/Guatemala}
      - JAVA_TOOL_OPTIONS=-Xmx384m -Duser.timezone=${TZ:-America/Guatemala}
    expose:
      - "8080"
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    depends_on:
      - backend
    ports:
      - "8081:80"
    restart: unless-stopped
